# Environment Configuration for Airbnb MCP Server

# Server Configuration
PORT=3000
NODE_ENV=production
MCP_MODE=http

# Authentication - comma-separated list of access tokens
MCP_ACCESS_TOKENS=your-secret-token-1,your-secret-token-2

# CORS Configuration - comma-separated list of allowed origins
ALLOWED_ORIGINS=https://your-frontend.com,https://another-domain.com

# Airbnb Configuration
IGNORE_ROBOTS_TXT=false

# Google Cloud Configuration (for deployment)
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1

# Optional: Server version override
MCP_SERVER_VERSION=0.1.3